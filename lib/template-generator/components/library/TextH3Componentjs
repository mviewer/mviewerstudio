import TemplateComponent from './TemplateComponent.js';

const header = {
  icon: 'bi bi-type-h3',
  title: 'Image',
};

const defaultTypeFields = ['field', 'multi', 'static'];

export class TextH3Component extends TemplateComponent {
  constructor() {
    super(header, defaultTypeFields, [
      {
        id: 'h3InputForm'
      },
    ]);
  }
}
